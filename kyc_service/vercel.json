{
  "version": 2,
  "builds": [
    {
      "src": "api/gateway.py",
      "use": "@vercel/python",
      "config": {
        "maxLambdaSize": "15mb"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/gateway.py"
    }
  ],
  "env": {
    "PYTHONPATH": "."
  }
}
